import { NbJoursCheckDirective } from './nb-jours-check.directive';

describe('NbJoursCheckDirective', () => {
  it('should create an instance', () => {
    const directive = new NbJoursCheckDirective();
    expect(directive).toBeTruthy();
  });
});
